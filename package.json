{
  "name": "novelsolar-bitrix24-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hosted MCP server connecting Claude to the NovelSolar Bitrix24 portal via REST API.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "dotenv": "^16.4.5",
    "express": "^5.2.1",
    "zod": "^3.25.0",
    "@types/express": "^5.0.1",
    "@types/node": "^20.14.0",
    "typescript": "^5.6.3"
  }
}
